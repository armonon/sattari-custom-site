// Store name for uploaded product photos.
//
// Lives here rather than in a function file so other server code (backups)
// can reference it without importing a Netlify handler.
export const IMAGE_STORE = 'catalog-images';
